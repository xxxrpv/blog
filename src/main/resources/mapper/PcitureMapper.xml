<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xrp.blog.mapper.PictureMapper">

    <!--查询所有照片-->
    <select id="listPictures" resultType="com.xrp.blog.pojo.Picture">
        select * from picture order by picture.id desc
    </select>

    <!--添加图片-->
    <insert id="savePicture" parameterType="com.xrp.blog.pojo.Picture">
        insert into picture (time,address,description)
        values (#{time},#{address},#{description})
    </insert>

    <!--根据id查询照片-->
    <select id="getPicture" resultType="com.xrp.blog.pojo.Picture">
        select * from picture p where p.id = #{id}
    </select>

    <!--编辑修改相册-->
    <update id="updatePicture" parameterType="com.xrp.blog.pojo.Picture">
        update picture
        set time = #{time}, address = #{address},description = #{description}
        where id = #{id};
    </update>

    <!--删除照片-->
    <delete id="deletePicture" >
        delete from picture where id = #{id}
    </delete>


</mapper>