<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xrp.blog.mapper.CommentMapper">


    <resultMap id="comment" type="com.xrp.blog.pojo.Comment">
        <id property="id" column="id"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="content" column="content"/>
        <result property="adminComment" column="admin_comment"/>
        <result property="avatar" column="avatar"/>
        <result property="createTime" column="create_time"/>
        <result property="blogId" column="blog_id"/>
        <result property="parentCommentId" column="parent_comment_id"/>
        <association property="blog" javaType="com.xrp.blog.pojo.Blog">
            <id property="id" column="bid"/>
        </association>
    </resultMap>

    <select id="findByBlogIdAndParentCommentNull" resultMap="comment">
        select *
        from t_comment c
        where blog_id = #{blogId} and parent_comment_id = #{blogParentId}
        order by create_time desc
    </select>
    <!--查询一级子评论-->
    <select id="findByBlogIdParentIdNotNull" resultType="Comment">
        select *
        from t_comment c
        where c.blog_id = #{blogId} and c.parent_comment_id = #{id}
        order by c.create_time desc
    </select>
    <!--查询二级子评论-->
    <select id="findByBlogIdAndReplyId" resultType="Comment">
        select *
        from t_comment c
        where c.blog_id = #{blogId} and c.parent_comment_id = #{childId}
        order by c.create_time desc
    </select>

    <insert id="saveComment" parameterType="com.xrp.blog.pojo.Comment">
        insert into t_comment (nickname,email,content,avatar,
        create_time,blog_id,parent_comment_id, admin_comment)
        values (#{nickname},#{email},#{content},#{avatar},
        #{createTime},#{blogId},#{parentCommentId}, #{adminComment});
    </insert>

    <select id="findByParentCommentId" resultMap="comment">
        select c.id , c.nickname, c.email, c.content, c.avatar,
        c.create_time, c.blog_id, c.parent_comment_id
        from t_comment c
        where c.parent_comment_id = #{parentCommentId}
    </select>

    <select id="findById">
        select *
        from t_comment c
        where c.parent_comment_id = #{parentCommentId}
    </select>

</mapper>